这里是一份**纯粹面向工程实现的拓扑变换规范**。它剥离了冗长的解释，将 `5.md` 的数学理论转化为可以直接映射为 Rust 代码的结构化指令。

---

# 终极拓扑变换规范 (工程实现蓝皮书)

## 0. 核心数据契约

在执行任何变换前，必须具备以下上下文：
- `root: LayoutNode`：当前布局树的根节点（可变引用）。
- `t1: &str`, `t2: &str`：被拖拽的两个叶子窗口的唯一 ID。
- `drag_dir: Direction`：拖拽方向 ($H$ 或 $V$)。$H$ 代表左右拖拽（产生垂直线），$V$ 代表上下拖拽（产生水平线）。
- `rect_map: &HashMap<String, WindowRect>`：拖拽前一刻的物理真值表（记录了所有节点的绝对像素坐标）。

**工程铁律**：任何变换产生的中间节点，必须在此步骤内根据 `rect_map` 计算并注入 `WindowSize::Percent`（万分比），**绝不允许在变换结束后再调用全局反算**。

---

## 1. 主入口算子 ($\mathcal{O}_1$)

**输入**：`root`, `t1`, `t2`, `drag_dir`, `rect_map`
**输出**：`Option<(String, String)>` (返回最终被拉平为直接兄弟的两个节点的 ID，用于后续物理覆盖注入。若返回 `None` 则表示拓扑未改变)。

**流程**：
1. 在 `root` 中查找包含 `t1` 和 `t2` 的最近公共父容器 $P$。
2. 在 $P$ 中定位包含 `t1` 的直接子节点 $C_{p1}$，以及包含 `t2` 的直接子节点 $C_{p2}$。
3. 判定分支：
   - **If $C_{p1} == C_{p2}$**: 执行 [§2. 容器内同级算子]，返回 `None`。
   - **If $C_{p1} \neq C_{p2}$**:
     - 检查 $P$ 的所有直接子节点方向。若 $\forall S \in P.children, \text{dir}_S \perp \text{drag\_dir}$，则执行 [§3. 网格提取算子]。
     - 否则，执行 [§4. 标准跨容器算子]。
4. 执行 [§5. 后置全局校验]。若校验失败，丢弃变更，返回 `None`。

---

## 2. $\mathcal{O}_{1,\text{intra}}$ (容器内同级算子)

**条件**：$t_1$ 和 $t_2$ 是直接兄弟。
**动作**：无需改变 AST 拓扑。直接返回 `None`，交由物理层注入 `Absolute` 覆盖并缩放。

---

## 3. $\mathcal{O}_{1,\text{extract}}$ (网格提取算子)

**条件**：跨容器拖拽，且 $P$ 的所有子容器方向均与 `drag_dir` 垂直。

**步骤**：
1. **索引对等校验**:
   - $k_1 = \text{index\_of}(C_{p1}.children, \text{containing}(t_1))$
   - $k_2 = \text{index\_of}(C_{p2}.children, \text{containing}(t_2))$
   - **If $k_1 \neq k_2$**: 拓扑不对称，中止变换，返回 `None`。
2. **提取与残余切分**:
   - 对于 $P$ 的所有子节点 $S_i$：
     - $ext_i = S_i.children[k_1]$
     - $res_i = S_i.children \setminus \{ext_i\}$
     - 若 $|res_i| == 0$，丢弃 $res_i$。若 $|res_i| == 1$，解包为其唯一子节点。
3. **正交分组包装**:
   - **Extracted 组**: $E_{list} = [ext_1, ext_2, \dots]$。沿 `drag_dir` 包装并执行 [§6. 同方向拉平]。
   - **Residual 组**: $R_{list} = [res_1, res_2, \dots]$（过滤空值）。沿 $\text{dir}_P$ 包装并执行 [§6. 同方向拉平]。
4. **最终组装**:
   - 根据 $ext_1$ 和 $res_1$ 在 `rect_map` 中的坐标，判定物理先后顺序。
   - 沿 $d_\perp$（与拖拽方向垂直的方向）将 Extracted 组和 Residual 组包装为节点 $R$，并执行 [§6. 同方向拉平]。
5. **比例锚定**:
   - 从 `rect_map` 获取 $R$ 及其两个子组的物理尺寸，计算占总尺寸的万分比，写入 AST 的 `percent` 字段。
6. **替换与返回**:
   - 将 $P$ 的父节点中指向 $P$ 的引用替换为 $R$。
   - 返回 `(Extracted_ID, Residual_ID)`。

---

## 4. $\mathcal{O}_{1,\text{standard}}$ (标准跨容器算子)

**条件**：跨容器拖拽，且 $P$ 中存在与 `drag_dir` 平行的子容器。

**步骤**：
1. **等高链收敛**:
   - 初始化目标集合 $L = \{t_1, t_2\}$。
   - 遍历 $L$ 中的节点，若其父容器方向等于 `drag_dir`，则将该父容器的所有叶子兄弟加入 $L$。
2. **三段切分与展平**:
   - 将 $C_{p1}$ 和 $C_{p2}$ 沿 `drag_dir` 递归展平（消除同方向嵌套）。
   - 根据 $L$，将展平后的 $C_{p1}$ 切分为 `pre_1`, `core_1`, `post_1`。
   - 同理切分 $C_{p2}$ 为 `pre_2`, `core_2`, `post_2`。
3. **交集防呆校验**:
   - **If $\text{pre}_1 \cap \text{pre}_2 \neq \emptyset$ 或 $\text{post}_1 \cap \text{post}_2 \neq \emptyset$**: 拓扑冲突，中止变换，返回 `None`。
4. **保形包装**:
   - $Core = \text{Container}(\text{drag\_dir}, \text{core}_1 \oplus \text{core}_2)$
   - $Pre = \text{CombineOrthogonal}(\text{pre}_1, \text{pre}_2, \text{drag\_dir}, \text{dir}_{C1}, \text{dir}_{C2})$ （*注：Combine 操作绝不强行配对，各自保持原方向包装，空组跳过，非空组沿 drag_dir 拼接*）。
   - $Post = \text{CombineOrthogonal}(\text{post}_1, \text{post}_2, \text{drag\_dir}, \text{dir}_{C1}, \text{dir}_{C2})$
5. **最终组装**:
   - 将 $[Pre, Core, Post]$ 中的非空节点沿 $\text{dir}_{C1}$ 包装为节点 $R$，并执行 [§6. 同方向拉平]。
6. **比例锚定**:
   - 根据 `rect_map`，计算 $R$ 及其子节点的物理尺寸占比，写入 `percent` 字段。
7. **替换与返回**:
   - 将 $P$ 的父节点中指向 $P$ 的引用替换为 $R$。
   - 返回 `(Core_1_Rep_ID, Core_2_Rep_ID)`。

---

## 5. 后置全局校验 ($\mathcal{F}_{post}$)

**条件**：AST 变更完成后，提交前的最后防线。
**校验项**：
- **Inv1 (无重复)**: 遍历新树，任何一个叶子 ID 必须只出现 1 次。
- **Inv3 (无丢失)**: 新树的叶子集合必须与旧树的叶子集合完全相等。
**动作**：任一校验失败，立即回滚所有 AST 变更，返回 `None`。

---

## 6. 辅助算子定义

1. **$\text{Flatten}(node)$ (同方向拉平)**:
   - 若 `node` 是容器，且其唯一子节点也是同方向的容器，则将子节点的 children 提升到 `node` 中。递归执行此操作以消除冗余嵌套。
   - 若 `node` 只有一个子节点，直接返回该子节点（解包单子节点容器）。
2. **$\text{CombineOrthogonal}(s_1, s_2, d, d_1, d_2)$ (正交包装)**:
   - 若 $s_1, s_2$ 均为空，返回空。
   - 若仅一方为空，将非空一方沿其原方向包装返回。
   - 若均非空，沿 $d$（拖拽方向）将 $[\text{wrap}(s_1, d_1), \text{wrap}(s_2, d_2)]$ 包装返回。
3. **$\text{CalcPercent}(node, parent\_size, rect\_map)$ (比例计算)**:
   - $size = \text{rect\_map}[node].width$ (若 $dir=H$) 或 $.height$ (若 $dir=V$)。
   - $percent = (size \times 10000 / parent\_size)$。
