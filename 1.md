# 全面代码审查报告

## 一、致命 Bug（必须立即修复）

### 1. `compute_rects` 中的无限循环
**文件**: `src/layout/mod.rs` 第 559-573 行

```rust
if remainder > 0 {
    let mut idx = 0;
    while remainder > 0 {
        let child_size = match &children[idx] { ... };
        // 只有非 Absolute 的节点才能吸收余数
        if !matches!(child_size, Some(WindowSize::Absolute(_))) {
            content_sizes[idx] += 1;
            remainder -= 1;
        }
        idx = (idx + 1) % children.len(); // 循环回到开头
    }
}
```

**问题**: 当容器内**所有子节点都是 `Absolute`** 且总和不能精确填满容器时，`remainder` 永远不会减少，`while remainder > 0` 变成**死循环**，程序完全卡死。后面的"极端兜底"分支（575-578行）永远无法到达。

**修复**: 在循环中检测"一轮完整遍历后 remainder 未减少"则 break。

---

### 2. `g` / `G` 键绑定冲突 — "跳到顶部"功能丢失
**文件**: `src/config/mod.rs` 第 141-155 行 + 第 37-43 行

```rust
// defaults 中:
("g", "__TOP__"), ..., ("G", "__BOTTOM__"),

// parse_key_desc 中:
let lower = desc.to_lowercase();  // "G" 被转成 "g"
// → KeyCode::Char('g'), KeyModifiers::NONE
```

**问题**: `parse_key_desc` 对所有输入做 `to_lowercase()`，导致 `"g"` 和 `"G"` 生成**完全相同的 `KeyBindingKey`**。HashMap insert 时 `G→__BOTTOM__` 覆盖了 `g→__TOP__`，**用户永远无法触发"跳到顶部"**。

**修复**: 对单字符输入保留原始大小写，仅在解析修饰键前缀时做小写化。

---

### 3. `apply_search` 破坏 `visible_ids` / `visible_depths` 平行数组不变量
**文件**: `src/app/mod.rs` 第 907-922 行

```rust
pub fn apply_search(&mut self, query: &str, ...) {
    if let Some(Component::Tree(t)) = self.components.get_mut(&focused_name) {
        let matched = search::match_entities(&t.dataset.entities, query);
        t.rebuild_visible_ids();           // 重建两个平行数组
        if !matched.is_empty() {
            t.visible_ids.retain(|id| matched.contains(id));  // 只过滤了一个！
            // visible_depths 没有同步过滤 ← BUG
            ...
        }
    }
}
```

**问题**: `visible_ids` 被过滤后，`visible_depths[i]` 不再对应 `visible_ids[i]`，导致渲染时缩进错乱。此外 `matched.is_empty()` 时静默失败，无任何反馈。且搜索后**无法清除过滤**（永久丧失未匹配节点），只能靠 reload 恢复。

---

## 二、死代码 / 未完成功能

### 4. `pending_percent_overrides` 整套机制是死代码
**文件**: `src/app/mod.rs` 第 88 行、`src/main.rs` 第 149-156 行

```rust
// 声明
pub pending_percent_overrides: HashMap<String, WindowSize>,

// main.rs 中 drain：
if !engine.pending_percent_overrides.is_empty() {
    for (name, size) in engine.pending_percent_overrides.drain() {
        engine.window_rect_overrides.insert(name, size);
    }
}

// handle_ipc_update 中 clear：
self.pending_percent_overrides.clear();
```

**问题**: 全代码库中**没有任何地方向 `pending_percent_overrides` 写入数据**。这个字段被声明、被清空、被 drain，但永远是空的。整个"延迟池"机制是一具空壳。

---

### 5. `LayoutLayer` 多个字段从未被使用

| 字段 | 位置 | 状态 |
|------|------|------|
| `LayoutLayer::name` | `layout/mod.rs:60` | 永远为 `None`，注释说"后续可从 node 中提取" |
| `LayoutLayer::runtime_rect_override` | `layout/mod.rs:65` | 永远为 `None`，从未被读写 |
| `LayoutNode::Window::border_chars` | `layout/mod.rs:38` | 解析时永远设为 `None` |
| `LayoutNode::Window::draggable` | `layout/mod.rs:39` | 被解析但拖拽系统完全无视它（改用物理碰撞检测） |
| `InputState::last_rendered` | `app/input.rs:9` | 从未被读取 |

---

### 6. `__TOGGLE_LAYOUT__` 单参数分支是死代码
**文件**: `src/main.rs` 第 284-287 行

```rust
"__TOGGLE_LAYOUT__" => {
    // 需要第二个参数，但单参数指令无法传递
    // 这里仅作占位，实际使用需通过 __ACTIVATE_INPUT__ 或外部 IPC
}
```

**问题**: 这个分支什么也不做，只是占位。真正生效的是第 343 行的双参数分支。

---

## 三、Hacky 代码（阻碍扩展）

### 7. `Anchor::ScreenAbsolute` 的百分比支持 — 50 行注释"讨论"
**文件**: `src/layout/mod.rs` 第 399-455 行

```rust
Anchor::ScreenAbsolute { x, y } => {
    // 注意：这里 x/y 可能是百分比暂存值
    // 但由于 parse_coord_value 无法区分 % 和绝对值，
    // 我们需要一个更聪明的方式。
    // 简化方案：如果 x <= 100 且原始字符串含 %，则视为百分比
    // 但解析阶段已经丢失了 % 信息。
    //
    // 【修正】：...
    // 【最终方案】：修改 Anchor 枚举，增加 Percent 变体
    // 但这需要改 parse_coord_value 的返回类型。
    // 为了本次重构的最小侵入性，我们采用一个折中：
    // ...
    // 不，这太 hacky 了。让我们直接改 Anchor 枚举。
    // 但由于时间关系，我们先用一个简单的启发式：
    // ...
    // 实际上，最好的方式是让 parse_coord_value 返回一个枚举。
    // 但我们先这样实现，后续可以优化。

    let actual_x = if *x <= 100 && term_width >= 80 {
        *x  // ← 什么都没做
    } else {
        *x
    };
```

**问题**: 50 行注释讨论了一个已知设计缺陷，最终什么也没做。`parse_coord_value` 丢弃了 `%` 信息，导致 `@(50%,50%)` 被当作绝对坐标 `(50, 50)` 处理。**整个浮动层百分比定位功能不工作**。

**修复**: 将 `Anchor::ScreenAbsolute { x: u16, y: u16 }` 改为 `ScreenAbsolute { x: Coord, y: Coord }`，其中 `Coord` 是 `enum Coord { Pixels(u16), Percent(u16) }`。

---

### 8. `layer.layout.layers[0].root` — 隐式单层假设
**文件**: `src/app/mod.rs` 多处（935, 994, 1032, 1218, 1238, 1258）

```rust
Self::find_sibling_sum_in_node(&layer.layout.layers[0].root, ...)
Self::extract_edges_from_node(&layer.layout.layers[0].root, ...)
Self::search_bbox_in_node(&layer.layout.layers[0].root, ...)
```

**问题**: 直接索引 `[0]` 假设每个 `LayoutLayerState` 内的 `Layout.layers` 恰好只有 1 个元素。这个不变量没有任何运行时检查，一旦 `Layout::parse` 返回多层数组就会 panic。

---

### 9. 三重簿记：`LayoutLayerState` vs `LayoutLayer` vs `Layout`
**文件**: `src/app/mod.rs` + `src/layout/mod.rs`

```rust
// layout/mod.rs
pub struct LayoutLayer {
    pub name: Option<String>,
    pub z_index: usize,          // ← 被忽略
    pub anchor: Anchor,
    pub root: LayoutNode,
    pub visible: bool,           // ← 被忽略
    pub runtime_rect_override: Option<WindowRect>,  // ← 被忽略
}

// app/mod.rs
pub struct LayoutLayerState {
    pub z_index: usize,          // ← 实际使用
    pub visible: bool,           // ← 实际使用
    pub layout: Layout,          // ← 包装 Layout，但只用 layers[0]
}
```

**问题**: `LayoutLayerState` 包装了 `Layout`，而 `Layout` 包含 `Vec<LayoutLayer>`，但只取 `[0]`。三层嵌套中，`LayoutLayer` 自身的 `z_index`、`visible`、`runtime_rect_override`、`name` 全部被忽略。这是历史重构的残留层。

---

### 10. `emit` 时间防抖与 `emit_select_if_changed` 状态防抖互相干扰
**文件**: `src/app/mod.rs` 第 360-433 行

```rust
pub fn emit(&mut self, signal: &str, ...) {
    if signal == "select" {
        if now.duration_since(*last).as_millis() < 200 {
            return;  // ← 200ms 内跳过
        }
    }
    // ... 实际发射
}

pub fn emit_select_if_changed(&mut self, ...) {
    let current_id = self.get_selected_entity().map(|e| e.id.clone());
    if current_id != self.last_emitted_select_id {
        self.last_emitted_select_id = current_id;
        self.emit("select", ...);  // ← 可能被 emit 内部的 200ms 防抖吞掉
    }
}
```

**问题**: `emit_select_if_changed` 检测到 ID 变化后调用 `emit("select")`，但 `emit` 内部的 200ms 时间防抖可能直接 return，导致**真正的选择变化信号被丢弃**。`last_emitted_select_id` 已经更新，所以下次即使 ID 没变也不会重发。信号丢失。

---

### 11. `engine.drag_active` 语义重载
**文件**: `src/main.rs` + `src/app/mod.rs`

```rust
// 拖拽调整大小时：
engine.drag_active = true;
engine.drag_resize_target = Some(DragTarget::ResizeEdge(...));

// 右键标记拖拽时：
engine.drag_active = true;  // ← 同一个字段
// drag_resize_target 保持 None
```

**问题**: 同一个布尔字段表示两种完全不同的模态（resize-drag vs mark-drag）。main.rs 中不得不用 `if engine.drag_active && engine.drag_resize_target.is_some()` 来区分。一旦逻辑写错，两种模态会互相干扰。应该用 `enum DragMode { None, Resize(...), Mark { start_idx, mode } }`。

---

### 12. `toggle_layout_visible` 名称匹配回退到数字索引
**文件**: `src/app/mod.rs` 第 307-322 行

```rust
pub fn toggle_layout_visible(&mut self, name: &str) {
    for layer in &mut self.layout_layers {
        if Self::layout_contains_window(&layer.layout, name) {
            layer.visible = !layer.visible;
            return;
        }
    }
    // 如果没找到匹配的图层，尝试按图层索引匹配（name 是数字字符串）
    if let Ok(idx) = name.parse::<usize>() {
        if let Some(layer) = self.layout_layers.get_mut(idx) {
            layer.visible = !layer.visible;
        }
    }
}
```

**问题**: 同一个参数 `name` 有时是窗口名，有时是图层索引（字符串数字）。调用者无法预测行为。如果窗口名恰好是数字（如 "1"），会优先匹配窗口而非索引。应该分两个 API：`toggle_by_window_name` 和 `toggle_by_index`。

---

### 13. `Layout::parse` 与 `parse_layouts` API 割裂 + `parse_layout` 冗余别名

```rust
// layout/mod.rs
impl Layout {
    pub fn parse(s: &str) -> Self { ... }       // 返回单层
}
pub fn parse_layout(s: &str) -> Layout {         // 别名，冗余
    Layout::parse(s)
}
pub fn parse_layouts(layout_args: &[String]) -> Layout {  // 返回多层
    ...
}
```

**问题**: 三个入口点，语义不同。`Layout::parse` 总是返回 `layers.len() == 1`，但 `parse_layouts` 可以返回多层。`parse_layout` 是纯冗余别名。而 `Engine::new` 中用 `parse_layout` 解析每个 `--layout` 参数，然后包装成 `LayoutLayerState`，但实际上 `parse_layouts` 已经能处理多参数合并。两套路径并存。

---

### 14. main.rs 中命令执行逻辑重复 4 次
**文件**: `src/main.rs` 第 289-336 行、319-335 行、353-399 行、369-398 行

```rust
// 以下块出现 2 次（单参数分支 + 多参数分支）：
if is_silent {
    match exec::execute_command_silent(&full_cmd_args) { ... }
    let mut drain_count = 0;
    while crossterm::event::poll(Duration::ZERO).unwrap_or(false) && drain_count < 100 {
        let _ = crossterm::event::read();
        drain_count += 1;
    }
} else {
    let _ = terminal::disable_raw_mode();
    let _ = stdout().execute(DisableMouseCapture);
    let _ = stdout().execute(terminal::LeaveAlternateScreen);
    let _ = stdout().execute(crossterm::cursor::Show);
    let _ = stdout().flush();
    let mut cmd = std::process::Command::new(&full_cmd_args[0]);
    cmd.args(&full_cmd_args[1..]);
    if let Ok(tty) = std::fs::OpenOptions::new()...open("/dev/tty") { ... }
    let status = cmd.status();
    // drain events again...
    // restore terminal...
    match status { ... }
}
```

**问题**: 完全相同的 40 行代码块出现 2 次（单参数分支和多参数分支各一次）。事件排空循环出现 4 次。应该抽取为 `fn execute_binding(engine: &mut Engine, args: &[String], is_silent: bool, term_size: ...) -> Result`。

---

### 15. `broadcast_selection_changed` 与 `init_views` 逻辑重复
**文件**: `src/app/mod.rs` 第 540-641 行

两个函数都做：遍历 View 组件 → 构建命令参数 → 执行 → 处理错误 → 写入 content_buffer。差异仅在 `selected_entity` 是否为 None。应该抽取为 `fn refresh_view(view: &mut ViewState, entity: Option<&Entity>, window_name: &str)`。

---

### 16. `broadcast_selection_changed` 的 `_term_width` / `_term_height` 未使用
**文件**: `src/app/mod.rs` 第 540 行

```rust
pub fn broadcast_selection_changed(&mut self, tree_name: &str, _term_width: u16, _term_height: u16) {
```

**问题**: 两个参数完全未使用，但所有调用点都必须传。是历史重构残留。

---

### 17. 边框开销计算逻辑分散在 3 处
| 位置 | 代码 |
|------|------|
| `layout/mod.rs:620-627` | `content_size()` 函数 |
| `ui/mod.rs:49-53` | `WindowRenderer::new()` 中的 match |
| `app/mod.rs:1429-1441` | `recalc_node_percentages_recursive()` 中的 match |

**问题**: 三处独立硬编码 `Box→2, Line→1/0, None→0`。修改边框语义需要同步改三处。应该统一使用 `layout::content_size()` 或 `BorderStyle::overhead()` 方法。

---

### 18. `pct_base` 边界情况：声明 0% 且无未声明子节点
**文件**: `src/layout/mod.rs` 第 524-528 行

```rust
let pct_base = if undeclared_count > 0 && declared_pct_sum <= 100 {
    100
} else {
    declared_pct_sum.max(1)  // ← 如果 declared_pct_sum == 0，pct_base = 1
};
```

**问题**: 如果所有子节点都声明了 `0%`（不太可能但可能），`pct_base = 1`，然后 `(flex_len * 0 / 1) = 0`，所有子节点尺寸为 0。极端情况但不优雅。

---

### 19. `exec::execute_command_args` 截断启发式
**文件**: `src/exec/mod.rs` 第 86-98 行

```rust
let final_line = if line.len() > MAX_LINE_CHARS * 4 {
    // 字节数 > 2000，认为含大量多字节字符
    let mut s: String = line.chars().take(MAX_LINE_CHARS - 3).collect();
    s.push_str("...");
    s
} else {
    let mut iter = line.chars();
    let s: String = iter.by_ref().take(MAX_LINE_CHARS - 3).collect();
    if iter.next().is_some() { format!("{}...", s) } else { line }
};
```

**问题**: `* 4` 是 UTF-8 最大字节数的启发式。如果一行恰好 600 个 ASCII 字符 + 1 个 4 字节 emoji，`line.len() = 604 < 2000`，走 else 分支，正确截断。但如果一行是 600 个 4 字节字符，`line.len() = 2400 > 2000`，走 if 分支，但 `chars().take(497)` 只取 497 个字符——过度截断。应该统一用字符数判断。

---

### 20. `style::add_rule` 正则检测不完整
**文件**: `src/style/mod.rs` 第 105-121 行

```rust
let matcher = if pattern.starts_with('^')
    || pattern.contains('*')
    || pattern.contains('.')
    || pattern.contains('+')
    || pattern.contains('?')
    || pattern.contains('[')
{
    match Regex::new(pattern) { ... }
} else {
    RuleMatcher::Exact(pattern.to_string())
};
```

**问题**: 遗漏了 `(`, `|`, `{`, `\` 等正则元字符。如果用户写 `type_(note|task)=cyan`，会被当作精确匹配，永远不命中。应该直接尝试 `Regex::new`，失败则降级。

---

### 21. `engine.focused` 直接从 main.rs 赋值
**文件**: `src/main.rs` 第 566 行

```rust
engine.focused = app::Focus::Component(name.clone());
```

**问题**: 外部直接修改内部状态，绕过任何焦点变更副作用（如 `focus` 信号发射）。应该用 `engine.set_focus(name)`。

---

### 22. 内部命令用魔术字符串
**文件**: 全局

```rust
"__EXIT__", "__ESC__", "__TAB__", "__UP__", "__DOWN__", "__EXPAND__",
"__MARK__", "__TOP__", "__BOTTOM__", "__ENTER__", "__ACTIVATE_SEARCH__",
"__ACTIVATE_CMD__", "__ACTIVATE_INPUT__", "__TOGGLE_LAYOUT__",
"__SHOW_LAYOUT__", "__HIDE_LAYOUT__", "__CANCEL__"
```

**问题**: 17 个魔术字符串散落在 main.rs、config/mod.rs、app/mod.rs 中。没有类型安全，拼写错误不会编译报错。应该用 `enum InternalCommand { Exit, Esc, Tab, ... }`。

---

## 四、性能问题

### 23. 每帧重复计算 `calc_all_rects`
**文件**: `src/main.rs` 第 169, 414, 470, 524, 555 行 + `src/app/mod.rs` 第 985, 1022 行

```rust
// main.rs 每帧：
let all_rects = engine.calc_all_rects(columns, rows);     // 第 1 次
engine.rebuild_draggable_edges(columns, rows);             // 内部第 2 次
// 拖拽时：
let all_rects_now = engine.calc_all_rects(columns, rows);  // 第 3 次
// 鼠标按下时：
let all_rects_before = engine.calc_all_rects(columns, rows); // 第 4 次
```

**问题**: `calc_all_rects` 遍历整个布局树并分配空间，每帧调用 2-4 次。应该每帧计算一次并缓存。

---

### 24. `rebuild_draggable_edges` 每帧 O(N²) 遍历
**文件**: `src/app/mod.rs` 第 981-1016 行 + `src/main.rs` 第 171 行

```rust
for i in 0..leaf_rects.len() {
    for j in i + 1..leaf_rects.len() {
        // 检查相邻关系
    }
}
```

**问题**: 每帧对所有叶子窗口做 N² 配对检查。布局变更不频繁，应该仅在布局树或 overrides 变化时重建。

---

### 25. `search::match_entities` 每次搜索全量 `to_lowercase`
**文件**: `src/search/mod.rs` 第 30-34 行

```rust
for entity in entities {
    let is_match = entity.id.to_lowercase().contains(&lower_query)
        || entity.display.to_lowercase().contains(&lower_query)
        || entity.path.to_lowercase().contains(&lower_query);
}
```

**问题**: 每次搜索都对所有字段做 `to_lowercase()`（分配新 String）。对于大数据集（数千实体），应该预计算小写索引。

---

## 五、架构问题（阻碍扩展）

### 26. `Engine` 是 God Object
**文件**: `src/app/mod.rs` 第 66-97 行

24 个字段，职责包括：
- 组件管理（`components`）
- 布局管理（`layout_layers`, `window_rect_overrides`, `pending_percent_overrides`）
- 拖拽状态（`drag_active`, `drag_mode`, `drag_start_idx`, `drag_resize_target`, `cached_edges`, `cached_intersections`）
- 焦点管理（`focused`, `main_tree_name`）
- 信号系统（`last_signal_emit`, `last_emitted_select_id`）
- 鼠标状态（`last_click_time`, `last_clicked_id`, `mouse_enabled`）
- 配置（`key_bindings`, `border_chars`）
- IPC / 错误（`last_error`, `global_relations`）

**建议拆分**: `LayoutEngine`, `DragController`, `SignalBus`, `FocusManager`, `ComponentRegistry`。

---

### 27. `Component` enum 无 trait 抽象
**文件**: `src/app/mod.rs` 第 49-56 行

```rust
pub enum Component {
    Tree(TreeState),
    View(ViewState),
    StatusBar(StatusBarState),
    Input(InputState),
}
```

**问题**: 每次操作都需要 match 所有变体。新增组件类型需要修改所有 match 分支。应该定义 `trait Component { fn handle_event(...); fn render(...); fn rect_changed(...); }`。

---

### 28. main.rs 主循环 500+ 行
**文件**: `src/main.rs` 第 143-690 行

单个 `'main_loop` 包含：终端尺寸检测、IPC 处理、布局计算、渲染、事件轮询、键盘处理（含输入模式、命令执行、TTY 切换）、鼠标处理（含拖拽、点击、双击、标记、滚动）。应该拆分为 `fn handle_key_event`, `fn handle_mouse_event`, `fn handle_resize` 等。

---

### 29. `LayoutLayerState` 的 `z_index` 与列表位置重复
**文件**: `src/app/mod.rs` 第 60-64 行

```rust
pub struct LayoutLayerState {
    pub z_index: usize,  // ← 与 layout_layers 中的索引相同
    pub visible: bool,
    pub layout: Layout,
}
```

**问题**: `z_index` 始终等于 `layout_layers` 中的位置索引（在 `Engine::new` 第 137 行 `z_index: i`）。冗余字段，且如果列表重排后 `z_index` 不会自动更新。

---

## 六、其他小问题

| # | 文件 | 问题 |
|---|------|------|
| 30 | `main.rs:146` | `terminal::size()` 失败时 `continue` 导致忙循环 |
| 31 | `main.rs:197-199` | poll 超时逻辑用 3 级 if-else，魔法数字 `1s/5s/10ms/50ms/200ms` 应为常量 |
| 32 | `app/mod.rs:858-898` | `handle_input_key` 遍历所有组件找 active input，O(N) |
| 33 | `app/mod.rs:854-856` | `has_active_input` 同样遍历，应维护 `active_input: Option<String>` |
| 34 | `app/mod.rs:900-905` | `activate_input` 覆盖 prefix，原始配置丢失 |
| 35 | `app/tree.rs:93-106` | `toggle_mark` 自动下移光标，行为隐晦 |
| 36 | `app/tree.rs:120-133` | `select_id` 找不到时静默回退到第一项，无错误反馈 |
| 37 | `layout/mod.rs:357-361` | 自动命名 `area_50pct` 可能冲突 |
| 38 | `layout/mod.rs:104-134` | `parse_layouts` 跳过空字符串，但 `Layout::parse` 对空字符串返回默认布局，行为不一致 |
| 39 | `exec/mod.rs:7-9` | `MAX_LINES/MAX_LINE_CHARS/MAX_TOTAL_BYTES` 硬编码，不可配置 |
| 40 | `exec/mod.rs:106-113` | stderr 读取在 stdout 循环之后，子进程可能已退出导致管道关闭 |
| 41 | `app/mod.rs:1228-1241` | `force_recalculate_percentages` 遍历所有图层，但只有被拖拽的图层需要重算 |
| 42 | `app/mod.rs:1267-1364` | `surgery_tree_node` 100 行树手术，无单元测试 |
| 43 | `app/mod.rs:295-301` | `calc_all_rects` 返回元组 `(WindowRect, String, BorderStyle, usize)`，应改为结构体 |
| 44 | `config/mod.rs:180-202` | `split_args` 不支持转义引号（如 `"a\"b"`） |
| 45 | `protocol/mod.rs:100-107` | 实体去重时用 `position` 线性查找，O(N²) |
| 46 | `ui/mod.rs:460` | `input.cursor as u16` — 如果 cursor 超过 u16 范围会截断（虽然不太可能） |
| 47 | `app/mod.rs:679-683` | `prepare_key_binding_args` 在焦点非 Tree 且无 main_tree 时返回 None，绑定静默失效 |

---

## 七、扩展建议优先级

| 优先级 | 行动项 |
|--------|--------|
| **P0** | 修复 `compute_rects` 死循环（#1）、`g/G` 键冲突（#2）、`apply_search` 数组不同步（#3） |
| **P1** | 抽取 main.rs 命令执行函数（#14）、清理 `pending_percent_overrides` 死代码（#4）、修复 `Anchor` 百分比支持（#7） |
| **P2** | 拆分 `Engine` God Object（#26）、定义 `Component` trait（#27）、统一边框开销计算（#17）、内部命令枚举化（#22） |
| **P3** | 帧级缓存 `calc_all_rects`（#23）、`rebuild_draggable_edges` 增量更新（#24）、合并 `LayoutLayerState`/`LayoutLayer`（#9） |
